[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22.16.0"
  NPM_VERSION = "11.19.0"
  NPM_FLAGS = "--package-lock=false --ignore-scripts --no-audit --no-fund"
